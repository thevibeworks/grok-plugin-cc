{
  "name": "grok-plugin-cc",
  "private": true,
  "type": "module",
  "description": "Grok plugin for Claude Code: reviews and task delegation through the Grok CLI",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
